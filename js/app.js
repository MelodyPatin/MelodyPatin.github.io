function init(){
    navigationBar.init();
    darkMode.init();
    scaleText.init();
    languagesBar.init();
    languagesThemes.init();
  }
  
  
  
  
  document.addEventListener("DOMContentLoaded",init);